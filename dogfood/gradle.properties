# Deliberately unlike the main build's: this one applies no pitest, so it is the only place in the
# repository where isolated projects can actually be switched on.
org.gradle.configuration-cache=true
