plugins {
    id 'io.github.joke.conventional-version' version '1.0.0'
}

rootProject.name = 'conventional-version'

include 'plugin'
include 'smoke-test'

project(':plugin').name = 'conventional-version'
